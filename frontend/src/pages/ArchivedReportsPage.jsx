import ReportsPage from './ReportsPage';

/** The archive is the same list, locked to archived matters and read-only. */
export default function ArchivedReportsPage() {
  return <ReportsPage archivedOnly />;
}
